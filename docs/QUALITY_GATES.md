# Quality gates

`npm run verify:full` is the local fail-fast gate. It verifies dependency policy, production audit, schema provenance, documentation, types, lint, tests, coverage, build, and package contents. Platform-native packaging, accessibility, and public-release verification run in the GitHub matrix.
