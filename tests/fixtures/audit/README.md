# ForgeLoopAudit scenario fixtures

These small, deterministic contracts name the audit scenarios covered by the
application and integration tests. They describe expected presentation
semantics; ForgeLoop remains the source of canonical task results.

Each `fixture.json` records the expected compatibility mode, canonical status,
and any quality or finding behavior that the scenario must preserve.
